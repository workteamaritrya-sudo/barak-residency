rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow any authenticated user to read
    match /{allPaths=**} {
      allow read: if true;
    }
    // Allow authenticated admin to write menu images
    match /menuImages/{fileName} {
      allow write: if request.auth != null;
    }
  }
}
